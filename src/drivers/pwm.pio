.program pwm_servo

.wrap_target
    pull block         ; packed: low ticks [31:16], high ticks [15:0]
    out x, 16
    out y, 16
    set pins, 1
high_loop:
    jmp x-- high_loop
    set pins, 0
low_loop:
    jmp y-- low_loop
.wrap
